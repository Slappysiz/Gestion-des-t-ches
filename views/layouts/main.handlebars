<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} – Gestionnaire de Tâches</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">

  <!-- Feather Icons -->
  <script src="https://unpkg.com/feather-icons"></script>

  <!-- Vos styles persos -->
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-50 text-gray-900 font-sans min-h-screen flex flex-col">

  {{> header}}

  {{!-- Bannière flash --}}
  {{#if successMessage}}
    <div
      id="flash-banner"
      class="mx-auto mt-4 max-w-3xl bg-green-100 border-l-4 border-green-500 text-green-700 p-4 rounded shadow"
      role="alert"
    >
      {{successMessage}}
    </div>
    <script>
      setTimeout(() => {
        const b = document.getElementById('flash-banner')
        if (b) b.style.display = 'none'
      }, 5000)
    </script>
  {{/if}}

  <main class="flex-1 w-full mx-auto px-4 sm:px-6 lg:px-8 py-6">
    {{!-- Affichage éventuel des erreurs --}}
    {{#if error}}
      <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4 rounded-md" role="alert">
        <p>{{error}}</p>
      </div>
    {{/if}}

    {{{body}}}
  </main>

  {{> footer}}

  <script>
    feather.replace();
  </script>
  <script src="/scripts/main.js"></script>
</body>
</html>
